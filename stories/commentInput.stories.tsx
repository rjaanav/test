import CommentInput from 'components/ui/commentInput';

export default {
  title: 'Rezonate/CommentInput',
  component: CommentInput,
};

const Template = args => <CommentInput {...args} />;

export const Default = Template.bind({});

import React from 'react';

import Dropdown from '../components/ui/dropdown';

export default {
  title: 'Rezonate/Dropdown',
  component: Dropdown,
};

const Template = args => <Dropdown {...args} />;

export const Demo = Template.bind({});
